<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Appdoctruyen_v2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Light mode -->
    <!-- Screen background if light mode on -->
    <style name="Screen">
        <item name="android:background">@color/white</item>
    </style>


    <style name="TextColor">
        <item name="android:textColor">#000000</item>
    </style>


    <!-- Toolbar background -->
    <style name="Toolbar">
        <item name="android:background">#00a4d6</item>
        <item name="android:textColor">@color/white</item>
    </style>


    <!-- Bottom Navigation -->
    <!-- Màu chữ và icon khi bình thường và khi được chọn -->
    <style name="BottomNavigation">
        <item name="itemTextColor">@color/nav_item_color</item>
        <item name="itemIconTint">@color/nav_item_color</item>
        <item name="android:background">@color/white</item>
    </style>


    <!-- Icon ngôi nhà trong activity_main_tim_kiem -->
    <style name="IconColor">
        <item name="tint">#03A9E0</item>
    </style>


    <!-- Icon trong Account fragment -->
    <style name="AccountIcon">
        <item name="android:tint">@color/black</item>
    </style>
</resources>